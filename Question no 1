 #include <iostream>
 #include <queue>
 using namespace std;
 int main() {
    queue<string> q;
    q.push("Grapes ");
    q.push("Potato");
    q.push("Tomato");
 // Display the queue
    queue<string> temp = q;
 while (!temp.empty()) {
        cout << temp.front() << " ";
        temp.pop();
    }
    cout << endl;
    // Dequeue elements
    cout << "Dequeued: " << q.front() << endl;
    q.pop();
    cout << "Dequeued: " << q.front() << endl;
    q.pop();
 // Display the queue again
    temp = q;
 while (!temp.empty()) {
        cout << temp.front() << " ";
        temp.pop();
    }
    cout << endl;
    return 0;
 }
